<form method="post" action="{{ path('app_foot_delete', {'idmatchh': foot.idmatchh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ foot.idmatchh) }}">
    <button class="btn">Delete</button>
</form>
