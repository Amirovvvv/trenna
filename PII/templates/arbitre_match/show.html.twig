
{% extends 'base.html.twig' %}

{% block body %}
<div style="display: flex;align-items:center;justify-content: center;margin-top: 150px">

<h1>ArbitreMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idarb</th>
                <td>{{ arbitre_match.idarb }}</td>
            </tr>
            <tr>
                <th>Nomarb</th>
                <td>{{ arbitre_match.nomarb }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ arbitre_match.specialite }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ arbitre_match.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arbitre_match_index') }}">back to list</a>

    <a href="{{ path('app_arbitre_match_edit', {'idarb': arbitre_match.idarb}) }}">edit</a>

    {{ include('arbitre_match/_delete_form.html.twig') }}
</div>
{% endblock %}
